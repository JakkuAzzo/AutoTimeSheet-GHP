# Disabled for now.
#
# This workflow was intended for a later admin-only publishing flow where approved
# job cards, task records, or calendar data could be committed into /data.
#
# It is not required for the current GitHub Pages trial app, and it was creating
# noisy failed Action runs while the static frontend is still being refined.
#
# Re-enable later when the admin publish workflow is ready to be tested properly.

name: Publish portal data disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Portal data publishing is disabled for the current static trial."
